sudo: required
services:
  - docker
  
before_install:
  - docker build -t sundar0989/test-docker-kubernetes -f Dockerfile.dev .

script:
  - docker run sundar0989/test-docker-kubernetes npm run test -- --coverage
